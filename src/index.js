import React from 'react';
import ReactDOM from 'react-dom';
import GiftRegistry from './GiftRegistry'; // Assuming GiftRegistry is the main component.

ReactDOM.render(
  <React.StrictMode>
    <GiftRegistry />
  </React.StrictMode>,
  document.getElementById('root')
);
